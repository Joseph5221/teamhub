# Copy to .env and fill in real values before running `docker compose up`.
# For plain `dotnet run` (not Docker), use user-secrets instead — see
# server/TeamHub.Server/README.md.

# Long random value used to sign JWTs. Never commit a real one.
JWT_SECRET=
